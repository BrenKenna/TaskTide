<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">
    <Appenders>
        <!-- Console Appender -->
        <Console name="STDOUT" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} %-5p [ %t -> %C.%M ]: %m%n"/>
        </Console>

        <!-- File Appender -->
        <File name="LOGFILE" fileName="logs/tasktide/TaskTide.log">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} %-5p [ %t -> %C.%M ]: %m%n"/>
        </File>
    </Appenders>

    <Loggers>
        <!-- Logger for specific package/class -->
        <Logger name="org.tasktide" level="INFO" additivity="false">
            <AppenderRef ref="STDOUT"/>
            <AppenderRef ref="LOGFILE"/>
        </Logger>

        <!-- Enable detailed config loading info -->
        <Logger name="io.smallrye.config" level="WARN" additivity="false">
            <AppenderRef ref="STDOUT"/>
            <AppenderRef ref="LOGFILE"/>
        </Logger>

        <!-- Enable CDI injection debug (Weld, etc) -->
        <Logger name="org.jboss.weld" level="WARN" additivity="false">
            <AppenderRef ref="STDOUT"/>
            <AppenderRef ref="LOGFILE"/>
        </Logger>

        <!-- Root fallback -->
        <Root level="INFO">
            <AppenderRef ref="STDOUT"/>
            <AppenderRef ref="LOGFILE"/>
        </Root>
    </Loggers>
</Configuration>