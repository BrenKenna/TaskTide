ext.configureTaskTidePom = {
    name = "TaskTide ${project.name}"
    description = project.description ?: "TaskTide ${project.name} library"
    url = 'https://tasktide.org'

    licenses {
        license {
            name = 'MIT License'
            url = 'https://opensource.org/licenses/MIT'
            distribution = 'repo'
        }
    }

    developers {
        developer {
            id = 'BrendanKenna'
            name = 'Brendan Kenna'
            url = 'https://github.com/BrenKenna/TaskTide'
        }
    }

    scm {
        connection = 'scm:git:git://github.com/BrenKenna/TaskTide.git'
        developerConnection = 'scm:git:ssh://git@github.com/BrenKenna/TaskTide.git'
        url = 'https://github.com/BrenKenna/TaskTide'
    }
}