plugins {
    id 'java'
    id 'com.github.johnrengelman.shadow' version '8.1.1' // Apply Shadow plugin for Fat JAR
}

description = 'tasktide:core'
group = 'org.tasktide.core'
version = '0.0.1'

dependencies {

    // Jakarta-NoSQL
    implementation "org.eclipse.jnosql.mapping:jnosql-mapping-document:1.1.6"
    implementation "org.eclipse.jnosql.databases:jnosql-mongodb:1.1.6"
    implementation "org.eclipse.jnosql.databases:jnosql-couchdb:1.1.6"
    implementation "org.eclipse.jnosql.databases:jnosql-arangodb:1.1.6"
    implementation "org.eclipse.jnosql.databases:jnosql-dynamodb:1.1.6"
    implementation "org.eclipse.jnosql.databases:jnosql-couchbase:1.1.6"


    // Jakarta CDI
    implementation 'jakarta.enterprise:jakarta.enterprise.cdi-api:4.1.0'

    // Config
    implementation 'org.eclipse.microprofile.config:microprofile-config-api:2.0'

    // JSON-B (de)Serialization
    implementation 'jakarta.json.bind:jakarta.json.bind-api:2.0.0' // For JSON-B serialization/deserialization
    implementation 'org.eclipse:yasson:3.0.3' // JSON-B implementation

    // Log4J
    implementation 'org.apache.logging.log4j:log4j-api:2.20.0'
    implementation 'org.apache.logging.log4j:log4j-core:2.20.0'

    // Use JUnit Jupiter for testing.
    testImplementation 'org.junit.jupiter:junit-jupiter:5.9.1'
    testImplementation 'org.apache.logging.log4j:log4j-api:2.20.0'
    testImplementation 'org.apache.logging.log4j:log4j-core:2.20.0'
    testImplementation 'org.apache.logging.log4j:log4j-slf4j2-impl:2.20.0'
    testImplementation 'org.jboss.weld:weld-junit5:4.0.3.Final'
}

jar {
    archiveBaseName.set("tasktide-${project.name}")
    archiveVersion.set(version)
}

tasks.register('fatJar', com.github.jengelman.gradle.plugins.shadow.tasks.ShadowJar) {
    archiveBaseName.set("tasktide-${project.name}")
    archiveVersion.set(version)
    archiveClassifier.set('all')
    configurations = [project.configurations.runtimeClasspath]
}

build.dependsOn fatJar

test {
    useJUnitPlatform()
}